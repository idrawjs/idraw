import { PrivateOptions } from './types';

export const defaultOptions: PrivateOptions = {
  width: 400,
  height: 300,
  contextWidth: 400,
  contextHeight: 300,
  devicePixelRatio: 1,
  onlyRender: false,
  maxRecords: 10,
}