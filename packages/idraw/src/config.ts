import { IDrawSettings } from '@idraw/types';

export const defaultSettings: Required<IDrawSettings> = {
  enableScroll: true,
  enableSelect: true,
  enableScale: true,
  enableRuler: true,
  enableTextEdit: true,
  enableDrag: false
};
